//
//  ViewController.swift
//  UITools
//
//  Created by niefeian on 09/03/2019.
//  Copyright (c) 2019 niefeian. All rights reserved.
//

import UIKit
import UITools

class ViewController: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()
        // Do any additional setup after loading the view, typically from a nib.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }

    @IBAction func touchUp(_ sender: Any) {
//        let pop = PopupController.create(self).customize([ .animation(.slideUp), .scrollable(false), .backgroundStyle(.blackFilter(alpha: 0.3)) ])
//        let vw = SubViewController()
////        vw.regCallBack {[weak self] (data) in
////            if let i  = data as? Int {
////                self?.reloadView(sender.superview, i: i)
////            }
////            pop.dismiss()
////        }
//        pop.show(vw)
        
    }
}

